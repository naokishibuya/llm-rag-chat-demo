{
  "name": "toy-finance-server",
  "version": "0.1.0",
  "description": "A toy MCP server exposing stock price lookup",
  "tools": [
    {
      "name": "get_stock_price",
      "description": "Return the latest mock stock price for a given symbol",
      "parameters": {
        "symbol": {
          "type": "string",
          "description": "Ticker symbol, e.g. AAPL or TSLA"
        }
      },
      "returns": {
        "type": "object",
        "properties": {
          "symbol": { "type": "string" },
          "price": { "type": "number" }
        }
      }
    }
  ]
}
